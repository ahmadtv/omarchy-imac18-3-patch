# DRAFT — not installed.   Target: /etc/sddm.conf.d/20-imac-greeter-gpu.conf
# OPTIONAL (see imac-greeter-compositor). Sorts after omarchy-settings'
# 10-wayland.conf, whose line it replaces:
#   CompositorCommand=start-hyprland -- --config /usr/share/sddm/hyprland.lua
# SDDM here: DisplayServer=wayland, greeter compositor = Hyprland (no X).
# Re-check after omarchy-settings updates in case 10-wayland.conf changes.

[Wayland]
CompositorCommand=/usr/local/bin/imac-greeter-compositor -- --config /usr/share/sddm/hyprland.lua
