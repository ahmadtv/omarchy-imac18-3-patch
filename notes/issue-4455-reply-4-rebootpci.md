@taprobane99 Thanks — and your `reboot=pci` was the clue. I tested it here as a control: the build from *before* any shutdown handling, warm-rebooted with `reboot=pci`, gives a straight Apple logo. So the hard PCI reset hides the woken tile, which is why you never see it. Two costs on this iMac18,3: a visibly longer black gap before the logo (the firmware re-inits more after a hard reset), and the ramoops region doesn't survive it, so shutdown/crash captures are lost. I'm keeping the driver-side handoff — straight logo at normal reboot speed — but that's the explanation for the difference between our machines, and anyone on `reboot=pci` doesn't need the handoff.

Correction to my numbers above: the file you counted (+779) already contained the reboot handoff — I folded it into the lean core after posting (the `amdgpu_pci_shutdown()` hunk plus the going-down gates). Since then I did a second pass on the whole core: the six per-role quirk flags are two, the nine inline helpers three, the two AUX-ready polls one shared helper, and `dpcd_set_link_settings()` is back to its mainline shape with a per-write retry instead of a rewritten function. Same behaviour, and the file at the link now counts **+355 code / +89 comment lines** including the handoff. It's what I boot daily. Your stripped 7.3-rc1 is close enough to converge; post it and I'll rebase onto it, and yes, expecting small adjustments per rc.

One more thing you'll see in your tree too, if you look: writing the wake latch pulses the slave tile's own HPD line, and the driver takes that as a plug event — full re-detect, hotplug to userspace, re-modeset, re-train, latch write, HPD again. Three rounds per bring-up here, each one a visible blink at the LUKS prompt and at session start, plus an occasional "enabling link 1 failed" from the re-train. Ignoring `DETECT_REASON_HPD` on a tiled slave that already has its sink fixes it: 0 rounds per boot since, link-loss recovery still comes in as HPD-RX. It's in the linked patch (one hunk in `link_detect()`).

-- Ahmad
